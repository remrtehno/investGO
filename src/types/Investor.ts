import type {User} from './User';

export declare namespace Investor {
  export type Portfolio = {
    balance: number,
    expected_revenue: string,
    in_investment: string,
    loans_count: string,
    missed_debts: string,
    num: number,
    paid_out: string,
    wait_debts: string
  }
}